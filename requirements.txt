# Core dependencies (Python standard library is sufficient for basic functionality)
# No external dependencies required for basic server operation

# Optional: For testing
requests>=2.31.0

# Optional: For enhanced features (future)
# jinja2>=3.1.0  # For advanced templating
# pyyaml>=6.0    # For YAML config support
